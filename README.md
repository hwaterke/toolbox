# toolbox

A monorepo of Node + TypeScript command line tools.

Packages live under `packages/`. There is no build step: Node 24 strips
TypeScript types natively, so every CLI runs straight from `.ts` source.

Contents and setup instructions are written up once the packages have landed.
